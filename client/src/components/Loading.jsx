import React from "react";
import ReactLoading from "react-loading";

const Loading = () => {
  return <div>
    <ReactLoading type="bubbles" color="#5C1B39"
        height={100} width={50} />
  </div>;
};

export default Loading;
